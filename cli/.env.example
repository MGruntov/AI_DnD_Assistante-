# Copy to .env and fill in values.
#
# Your production Worker base URL (no trailing slash), e.g.
# ADA_WORKER_URL=https://backend.ada-assistante.workers.dev
ADA_WORKER_URL=

# Must match the ARCHITECT_SECRET configured on the Worker
ARCHITECT_SECRET=
