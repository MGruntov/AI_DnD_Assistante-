// Bootstrap module for the ADA frontend.
// For now we keep `speech.js` as the main implementation, but this module exists
// so we can incrementally move code out into focused ES modules.
import '../speech.js';
