# Local dev placeholders (Cloudflare Workers uses Wrangler vars/secrets)
#
# Copy to `.env` for local tooling if needed, but do NOT commit real secrets.

# Backend:
GEMINI_API_KEY=
JWT_SECRET=
ARCHITECT_SECRET=
